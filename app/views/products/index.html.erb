<h1>Products</h1>
<%= link_to "New product", products_new_path %>
<div id="products"> 
  <% @products.each do |product|%>
    <div class="product">
        <a href="/products/<%= product.id %>">
          <%= product.name %>
        </a>
    </div>
  <% end %>
</div>